rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // 誰でも読み取り可能
    match /{allPaths=**} {
      allow read: if true;
    }

    // 認証ユーザーのみ書き込み、更新、削除が可能
    match /{allPaths=**} {
      allow write: if request.auth != null;
    }
  }
}
